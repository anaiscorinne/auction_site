<h1>Welcome to <%= @user.name %>'s Products' Page</h1>
<ul>
	<% @products.each do |product| %>
		<li>
			<h4><%= link_to("#{product.title}", "/products/#{product.id}") %> | <%= product.deadline %></h4>
		</li>
	<% end %>
</ul>
